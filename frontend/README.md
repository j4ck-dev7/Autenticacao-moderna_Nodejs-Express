# Frontend (React)

Minimal React frontend for the authentication backend.

Quick start:

```bash
cd frontend
npm install
npm start
```

The dev server proxy is configured to `http://localhost:5000` in `package.json`.
